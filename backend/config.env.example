# Exemple de configuration d'environnement pour backend
# Copiez ce fichier en `config.env` et adaptez les valeurs avant de démarrer

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=tech_info_plus

# Options applicatives
SECRET_KEY=change_me_to_secure_value
DEBUG=True

# Options additionnelles
# FRONTEND_BUILD_PATH=../frontend/build   # Optionnel: redéfinir le chemin du build frontend
